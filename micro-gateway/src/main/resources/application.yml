server:
  port: 8264
  servlet:
    context-path: /

spring:
  application:
    name: micro-gateway

eureka:
  host: 127.0.0.1
  client:
    service-url:
      defaultZone: http://${eureka.host:localhost}:${eureka.port:8182}/eureka/